{
    "version": 2,
    "builds": [
      {
        "src": "src/index.js",
        "use": "@vercel/node"
      }
    ],
    "build": {
      "env": {
        "RUBY_VERSION": "2.7.1"
      },
      "command": "npm install && gem install jekyll && jekyll build"
    }
  }
  